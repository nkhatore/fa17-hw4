<table>
  <tr>
    <th>Name</th>
  </tr>
  <%= @cats.each do |cat| %>
    <tr>
      <td><%= cat.name %></td>
    </tr>
  <% end %>
</table>

<table>
  <tr>
    <th>Name</th>
  </tr>
  <%= @users.each do |user| %>
    <tr>
      <td><%= user.name %></td>
    </tr>
  <% end %>
  <tr>
    <th>Email</th>
  </tr>
  <%= @users.each do |user| %>
    <tr>
      <td><%= user.email %></td>
    </tr>
  <% end %>
  <tr>
    <th>Age</th>
  </tr>
  <%= @users.each do |user| %>
    <tr>
      <td><%= user.age %></td>
    </tr>
  <% end %>
</table>

<table>
  <tr>
    <th>Todos</th>
  </tr>
</table>
